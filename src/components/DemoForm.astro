---
// Conversion / Demo Request section
---

<section class="py-20 lg:py-32 bg-primary text-white" id="demo">
  <div class="max-w-6xl mx-auto px-6">
    <div class="max-w-2xl mx-auto text-center">
      <h2 class="text-4xl lg:text-5xl font-extrabold mb-6">
        See How This Works<br>for Your Business
      </h2>
      <p class="text-lg text-gray-400 mb-12 max-w-lg mx-auto">
        A 30-minute conversation with a product expert. 
        No sales pitch — we'll show you how your specific operations 
        would work inside TravelOps.
      </p>
      
      <!-- Demo Form -->
      <form class="text-left bg-white/5 border border-white/10 rounded-xl p-8" id="demoForm">
        <div class="grid gap-4 sm:grid-cols-2 mb-4">
          <div class="flex flex-col gap-2">
            <label for="name" class="text-sm font-medium text-gray-300">Your Name</label>
            <input 
              type="text" 
              id="name" 
              name="name" 
              required 
              placeholder="Full name"
              class="px-4 py-3 bg-white/10 border border-white/20 rounded-md text-white placeholder:text-gray-500 focus:outline-none focus:border-accent focus:bg-white/15 transition-all"
            />
          </div>
          <div class="flex flex-col gap-2">
            <label for="company" class="text-sm font-medium text-gray-300">Company Name</label>
            <input 
              type="text" 
              id="company" 
              name="company" 
              required 
              placeholder="Your travel company"
              class="px-4 py-3 bg-white/10 border border-white/20 rounded-md text-white placeholder:text-gray-500 focus:outline-none focus:border-accent focus:bg-white/15 transition-all"
            />
          </div>
        </div>
        
        <div class="grid gap-4 sm:grid-cols-2 mb-4">
          <div class="flex flex-col gap-2">
            <label for="email" class="text-sm font-medium text-gray-300">Work Email</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              required 
              placeholder="you@company.com"
              class="px-4 py-3 bg-white/10 border border-white/20 rounded-md text-white placeholder:text-gray-500 focus:outline-none focus:border-accent focus:bg-white/15 transition-all"
            />
          </div>
          <div class="flex flex-col gap-2">
            <label for="phone" class="text-sm font-medium text-gray-300">Phone Number</label>
            <input 
              type="tel" 
              id="phone" 
              name="phone" 
              placeholder="+91 98765 43210"
              class="px-4 py-3 bg-white/10 border border-white/20 rounded-md text-white placeholder:text-gray-500 focus:outline-none focus:border-accent focus:bg-white/15 transition-all"
            />
          </div>
        </div>
        
        <div class="grid gap-4 sm:grid-cols-2 mb-4">
          <div class="flex flex-col gap-2">
            <label for="trips" class="text-sm font-medium text-gray-300">Trips per Year (Approx)</label>
            <select 
              id="trips" 
              name="trips"
              class="px-4 py-3 bg-white/10 border border-white/20 rounded-md text-white focus:outline-none focus:border-accent cursor-pointer"
            >
              <option value="" class="bg-gray-800">Select range</option>
              <option value="50-100" class="bg-gray-800">50 - 100</option>
              <option value="100-300" class="bg-gray-800">100 - 300</option>
              <option value="300-500" class="bg-gray-800">300 - 500</option>
              <option value="500+" class="bg-gray-800">500+</option>
            </select>
          </div>
          <div class="flex flex-col gap-2">
            <label for="team" class="text-sm font-medium text-gray-300">Team Size</label>
            <select 
              id="team" 
              name="team"
              class="px-4 py-3 bg-white/10 border border-white/20 rounded-md text-white focus:outline-none focus:border-accent cursor-pointer"
            >
              <option value="" class="bg-gray-800">Select range</option>
              <option value="5-15" class="bg-gray-800">5 - 15</option>
              <option value="15-50" class="bg-gray-800">15 - 50</option>
              <option value="50-100" class="bg-gray-800">50 - 100</option>
              <option value="100+" class="bg-gray-800">100+</option>
            </select>
          </div>
        </div>
        
        <div class="flex flex-col gap-2 mb-6">
          <label for="challenge" class="text-sm font-medium text-gray-300">Biggest Operational Challenge Right Now</label>
          <textarea 
            id="challenge" 
            name="challenge" 
            rows="3" 
            placeholder="What's breaking in your operations?"
            class="px-4 py-3 bg-white/10 border border-white/20 rounded-md text-white placeholder:text-gray-500 focus:outline-none focus:border-accent focus:bg-white/15 transition-all resize-none"
          ></textarea>
        </div>
        
        <button 
          type="submit" 
          class="w-full py-4 bg-accent text-white rounded-md font-semibold text-base hover:bg-accent-dark transition-colors"
        >
          Request Demo
        </button>
        
        <p class="text-center text-sm text-gray-500 mt-4">
          We'll respond within 24 hours. No automated sequences — a real person will reach out.
        </p>
      </form>
    </div>
  </div>
</section>

<script>
  const demoForm = document.getElementById('demoForm') as HTMLFormElement;
  
  demoForm?.addEventListener('submit', (e) => {
    e.preventDefault();
    
    const button = demoForm.querySelector('button[type="submit"]') as HTMLButtonElement;
    const originalText = button.textContent;
    
    button.textContent = 'Sending...';
    button.disabled = true;
    
    // Simulate submission
    setTimeout(() => {
      demoForm.innerHTML = `
        <div class="text-center py-12">
          <div class="text-6xl mb-4">✓</div>
          <h3 class="text-2xl font-bold text-white mb-4">Thank You!</h3>
          <p class="text-gray-400 mb-6">We've received your demo request. A product expert will reach out within 24 hours.</p>
          <p class="text-gray-500 text-sm">In the meantime, feel free to explore the rest of the platform overview above.</p>
        </div>
      `;
    }, 1500);
  });
</script>